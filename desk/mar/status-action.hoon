/-  status
|_  =action:status
++  grab
  |%
  ++  noun  action:status
  :: TODO json mark
  ++  json
    =,  dejs:format
    %-  of
    :~
      :: [%set-status (ot ~[location+so note+so activity+(se %tas)])]
      [%set-status (ot ~[location+so note+so activity+so])]
    ==
  --
++  grow
  |%
  ++  noun  action
  --
++  grad  %noun
--
